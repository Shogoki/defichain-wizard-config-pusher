"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isSeedValid = exports.isStringNullOrEmpty = void 0;
/**
 * Checks if a certain String is null or empty
 *
 * @param value the text to be checked
 * @returns true or false
 */
const isStringNullOrEmpty = (value) => {
    return value === undefined || value.length === 0;
};
exports.isStringNullOrEmpty = isStringNullOrEmpty;
/**
 * This is just a simple check to see if a seed might be valid.
 *
 * The real check will happen when the seed is encrypted.
 *
 * @param seed The seed to check.
 * @returns true if the seed seems to be valid.
 */
const isSeedValid = (seed) => !isStringNullOrEmpty(seed) && seed.length === 24;
exports.isSeedValid = isSeedValid;
